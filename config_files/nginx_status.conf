server {
    listen 8080;                     # choose a port that’s free
    server_name localhost;

    location /nginx_status {
        stub_status;                 # enables built-in status page
        allow 127.0.0.1;             # only allow localhost by default
        deny all;
    }
}